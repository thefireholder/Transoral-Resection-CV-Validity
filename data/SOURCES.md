# Where every number in data/ comes from

Generated by scripts/collect_results.py -- do not edit by hand.
`/u/sl257/shared_data` is a symlink to `/projects/illinois/cimed/bts/gayed/data`, so both spellings are the same files.

## maskrcnn

| what | file | note |
|---|---|---|
| reported per-class / aggregate metrics | `-` | n/p: SUPERSEDED. data/predictions/maskrcnn_test.json now holds the re-trained model (scripts/rerun/maskrcnn_train_curves.py --adopt, see scripts/rerun/output/maskrcnn/recipe.txt); its metrics are the source=recomputed rows. The old 5-epoch (am232) numbers remain in /u/sl257/shared_data/train_script/maskrcnn_project/run_output/mask_metrics/mask_metrics_summary.json. |
| training curve (original 5-epoch run) | `/projects/illinois/cimed/bts/gayed/data/result/data/training_curves/maskrcnn_original5ep.csv` | per-epoch train loss + COCO val mask AP transcribed from the deleted slurm_9815937_full.out. Only used if maskrcnn_rerun.csv is absent. |
| recomputed metrics (ap_method=coco) | `/projects/illinois/cimed/bts/gayed/data/result/data/computed/maskrcnn_test.json` | scripts/compute_mask_metrics.py on data/predictions/ -- identical matching + AP rule for every model. |

## yolo

| what | file | note |
|---|---|---|
| per-class Mask AP50, AP50:95 (test) | `/projects/illinois/cimed/bts/gayed/data/result/some disorganized results/1200 images/yolo-1200/test result/result.txt` | stdout of model.val(split='test') -- the per-class Mask mAP columns are ONLY in the log; 1200images_test/results.csv has box mAP only. ultralytics AP rule (linear ramp to (1,0)). |
| per-class Mask precision / recall (test) | `/u/sl257/shared_data/train_script/yolov12_project/runs/segment/1200images/1200images_test/results.csv` | Mask-P / Mask-R columns; ultralytics reports P/R at the F1-maximising confidence. |
| aggregate (test) | `/projects/illinois/cimed/bts/gayed/data/result/some disorganized results/1200 images/yolo-1200/test result/result.txt` | '=== Test metrics ===' block: metrics/*(M). Same as gathered yolo-1200/test result/result.txt. |
| per-class IoU / Dice | `-` | n/p from ultralytics; -> source=recomputed (scripts/export_yolo_predictions.py + compute_mask_metrics.py). |
| training curve | `/projects/illinois/cimed/bts/gayed/data/result/some disorganized results/1200 images/yolo-1200/val result/results.csv` | ultralytics per-epoch log, 60 epochs, seed 0, deterministic. Validation-split mask P/R/mAP per epoch; train-split P/R is never evaluated by ultralytics -> n/p. Rerun scripts/rerun/yolo_train_curves.py to fill. train/val loss = box+seg+cls+dfl. |
| training curve (RERUN, overrides the log-parsed one) | `/projects/illinois/cimed/bts/gayed/data/result/data/training_curves/yolo_rerun.csv` | written by scripts/rerun/; delete/rename this file to fall back to the original log. |
| recomputed metrics (ap_method=coco) | `/projects/illinois/cimed/bts/gayed/data/result/data/computed/yolo_test.json` | scripts/compute_mask_metrics.py on data/predictions/ -- identical matching + AP rule for every model. |

## sam3_text

| what | file | note |
|---|---|---|
| reported metrics | `-` | n/p by design: this condition (zero-shot, class-name text prompts, scripts/rerun/sam3/sam3_text_prompt.py) was never evaluated by an original script; see source=recomputed. |
| training curve | `-` | n/p: zero-shot, not trained. |

## sam3_yolobox

| what | file | note |
|---|---|---|
| reported metrics | `-` | n/p by design: two-stage YOLO boxes -> SAM3 masks (scripts/rerun/sam3/sam3_yolo_box_prompt.py); see source=recomputed. |
| training curve | `-` | n/p: SAM3 is not trained; the boxes come from the YOLO run. |

## sam3_gtbox

| what | file | note |
|---|---|---|
| per-class AP50, AP50:95 (test) | `/projects/illinois/cimed/bts/gayed/data/result/some disorganized results/1200 images/sam3-1200/per_class_ap.json` | sam3_model.py, ultralytics-style ap_per_class with every prediction conf=1.0 (one mask per GT box prompt, pad 5%). Same file as gathered sam3-1200/per_class_ap.json. |
| per-class + mean IoU / Dice (test) | `/projects/illinois/cimed/bts/gayed/data/result/some disorganized results/1200 images/sam3-1200/result.txt` | 'Per-class IoU / Dice' block; mean over ALL 983 GT instances (not just matched). Same as gathered sam3-1200/result.txt. |
| precision / recall, training curve | `-` | n/p: not a trained model; and with conf=1.0 for every prediction P/R collapse to a single point (precision = fraction of prompts with IoU>=0.5). |
| PR curve numbers | `-` | n/p from the original run (only PR_curve.png was saved); scripts/rerun/sam3/sam3_gt_box_prompt.py regenerates them -> source=recomputed. |
| recomputed metrics (ap_method=coco) | `/projects/illinois/cimed/bts/gayed/data/result/data/computed/sam3_gtbox_test.json` | scripts/compute_mask_metrics.py on data/predictions/ -- identical matching + AP rule for every model. |

## monai

| what | file | note |
|---|---|---|
| reported per-class / aggregate metrics | `-` | n/p: SUPERSEDED. data/predictions/monai_test.json holds the UNet re-trained on the COCO-derived masks (scripts/rerun/monai/monai_train_curves.py, recipe in scripts/rerun/output/monai/recipe.txt); its metrics are the source=recomputed rows. The old unified-vocabulary numbers remain in /projects/illinois/cimed/bts/gayed/data/result/some disorganized results/1200 images/monai-results/_result_map_iou_dice_per_class.csv. |
| training curve (RERUN, overrides the log-parsed one) | `/projects/illinois/cimed/bts/gayed/data/result/data/training_curves/monai_rerun.csv` | written by scripts/rerun/; delete/rename this file to fall back to the original log. |
| recomputed metrics (ap_method=coco) | `/projects/illinois/cimed/bts/gayed/data/result/data/computed/monai_test.json` | scripts/compute_mask_metrics.py on data/predictions/ -- identical matching + AP rule for every model. |

